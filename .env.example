# Copy to .env.local and fill in values.

# Firebase (required for auth and Firestore)
FIREBASE_PROJECT_ID=
FIREBASE_CLIENT_EMAIL=
FIREBASE_PRIVATE_KEY=
FIREBASE_STORAGE_BUCKET=

# Google Maps (optional; used for drive time and places)
GOOGLE_MAPS_API_KEY=
VITE_GOOGLE_MAPS_API_KEY=

# Google Calendar integration (optional)
# 1. In Google Cloud Console: enable Google Calendar API for the project.
# 2. Create OAuth 2.0 Client ID (Web application).
# 3. Add authorized redirect URI: https://yourdomain.com/api/auth/google-calendar/callback
#    (and for dev: http://localhost:5000/api/auth/google-calendar/callback or your dev URL).
GOOGLE_CALENDAR_CLIENT_ID=
GOOGLE_CALENDAR_CLIENT_SECRET=
# Base URL for OAuth redirect (e.g. https://yourdomain.com or http://localhost:5000)
BASE_URL=
# App URL for job links in Google Calendar event descriptions (e.g. https://app.yourdomain.com or http://localhost:5000). Falls back to BASE_URL if not set.
APP_URL=

# Xero integration (optional)
# 1. Register at https://developer.xero.com/app/manage, create OAuth2 app (Auth Code)
# 2. Add redirect: https://yourdomain.com/api/auth/xero/callback (and dev URL)
XERO_CLIENT_ID=
XERO_CLIENT_SECRET=
